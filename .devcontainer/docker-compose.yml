version: '3.8'
services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: \bundle-flow
    volumes:
      - ..:/bundle-flow:cached
    command: sleep infinity
    # VSCodeのforwardPortsを使うならports指定は不要
    gpus: all
